<%= form_for(@criteria_group) do |f| %>
  <% if @criteria_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@critera_group.errors.count, "error") %> prohibited this critera_group from being saved:</h2>

      <ul>
      <% @critera_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :criteria_id %><br />
    <%= f.number_field :criteria_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
