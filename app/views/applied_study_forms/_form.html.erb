<%= form_for([@student, @applied_study_form]) do |f| %>
  <% if @applied_study_form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@applied_study_form.errors.count, "error") %> prohibited this applied_study_form from being saved:</h2>

      <ul>
      <% @applied_study_form.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
      <%= f.hidden_field :student_id, value: @student.id %>
  </div>

  <div class="field">
    <%= f.label :studies_and_scales %><br />
    <%= f.text_field :studies_and_scales %>
  </div>

  <div class="actions">
    <%= f.submit value: "Create Applied Study Form", class: 'btn btn-primary'%>
  </div>
<% end %>


