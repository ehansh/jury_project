<% if current_user.is_a? Student %>
	<h1>Editing student</h1>

	<%= render 'form' %>

	<%= link_to 'Show', @student %> |
	<%= link_to 'Back', students_path %>

<% else %>
	Judges and Students cannot edit students!
<% end %>
