<p id="notice"><%= notice %></p>

<div>
  <div id="tabs">
    <ul class="nav nav-tabs" data-tabs="tabs">
      <li class="active"><a href="#home" data-toggle="tab">Student Information</a></li>
      <li><a href="#repertoire" data-toggle="tab">Repertoire</a></li>
      <li><a href="#applied_study_form" data-toggle="tab">Applied Study Forms</a></li>
      <% if current_user.is_a? Judge %>

      <% else %>
      <li><a href="#juries" data-toggle="tab">Juries</a></li>
      <% end %>
   </ul>
  </div>

  <div id="my-tab-content" class="tab-content">

    <div class="tab-pane active" id="home">
      <%= render 'info' %>
    </div>

    <div class="tab-pane" id="repertoire">
      <%= render 'repertoire' %>
    </div>

    <div class="tab-pane" id="applied_study_form">
      <%= render 'applied_study_forms/index' %>
    </div>

    <% if current_user.is_a? Judge %>

    <% else %>

    <div class="tab-pane" id="juries">
      <%= render 'juries_info' %>
    </div>

    <% end %>

  </div>
</div>


