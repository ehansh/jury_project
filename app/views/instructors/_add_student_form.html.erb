<div>

	<%= form_for @instructor do |f| %>

		<div class="field">
    		<%= f.label :student_id, "Add a Student:" %> 
    		<%= f.collection_select :student_id, Student.all, :id, :name %>
		</div>

		<div class="actions">
    		<%= f.submit %>
 		</div>

  	<% end %>

</div>